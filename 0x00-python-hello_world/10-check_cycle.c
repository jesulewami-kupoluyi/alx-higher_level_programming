#include<stdio.h>
#include<stdlib.h>
#include<lists.h>
/**
 * list:inputs values
 * int check_cycle: function that prints numbers
 * return:0 if there is no cycle, 1 if there is a cycle
 */
int check_cycle(listint_t *list)
{
list 
	list1 -> list;
	list2 -> list1;
}
